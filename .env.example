# Copy to `.env` and set values. Do not commit `.env`.

DUNE_API_KEY=
# Comma-separated query IDs (optional). Used only with: python3 etl.py --dune latest|execute
# Prefer `latest` to avoid burning execution credits; `execute` runs a fresh job.
DUNE_QUERY_IDS=
